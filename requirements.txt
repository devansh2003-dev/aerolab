# Top-level deps pinned to what we tested locally. Bump deliberately;
# GitHub Actions runs pytest on every push, but mismatches between Cloud's
# resolved versions and ours could still surface in production -- pin both
# here and in pyproject.toml.
streamlit==1.57.0
aerosandbox==4.2.9
neuralfoil==0.3.2
matplotlib==3.10.9
plotly==6.7.0
numpy==2.4.4
numba==0.65.1
# scipy used in the LBM render path for gaussian_filter (smooths vorticity +
# velocity fields) and for the body-outline expansion math.
scipy==1.17.1
# pandas pulled in via aerosandbox; pinned explicitly so the Cloud doesn't
# silently grab a version that breaks the polar CSV export.
pandas==3.0.2
# Pillow used to assemble the LBM-mode animated GIF (quantize + save_all).
# Pinned exactly to match the version the test suite + Cloud deploy run
# against -- a passing test build on 10.x doesn't guarantee a passing build
# on 12.x if anything in the quantize path changes.
Pillow==12.2.0
# scikit-image: silhouette extraction from uploaded PNG/JPG (Otsu threshold,
# find_contours, Douglas-Peucker polygon approximation). Pulled in with
# imageio/tifffile/networkx/lazy-loader -- adds ~30 MB to the Cloud cold-start
# but it's cached after the first deploy.
scikit-image==0.26.0
# (streamlit-drawable-canvas was previously pinned for the in-browser
# polygon drawer. It is no longer imported anywhere -- the draw-your-own-
# shape path uses components/polygon_drawer/ as a self-contained
# component. Removed at v1.7.0 cleanup to drop ~ 5 MB from Cloud
# cold-start. Do not re-add unless you are wiring up a new canvas
# surface that actually imports it.)
# Note: pillow-heif (HEIC) and pillow-avif-plugin (AVIF) were briefly
# pinned here but kept failing Cloud's pip resolution step. Native
# PIL formats (PNG / JPG / GIF / BMP / TIFF / WEBP / ICO / PPM / TGA)
# cover 99 % of real uploads -- if you need HEIC, convert it to PNG
# in your phone's share sheet first.
